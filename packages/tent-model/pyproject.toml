[project]
name = "tent-model"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "beanie>=2.0.1,<3",
    "pydantic>=2.0",
    "pymongo>=4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tent_model"]
