[project]
name = "tent-python"
version = "0.0.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["packages/tent-model", "worker"]

[dependency-groups]
dev = ["pytest>=8.0", "pytest-asyncio>=0.24"]
