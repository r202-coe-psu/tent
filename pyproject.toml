[project]
name = "tent-python"
version = "0.0.0"
requires-python = ">=3.14"

[tool.uv.workspace]
members = ["packages/tent-model", "worker", "backend"]

[dependency-groups]
dev = ["pytest>=8.0", "pytest-asyncio>=0.24", "ruff>=0.9.5"]
