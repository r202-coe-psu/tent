---
scope: Volunteer job sub-shift identity and assignment pipeline
purpose: Build substrate and implementation approval plan for schema, API, frontend, backend, worker, migration, and tests
altitude: feature
updated: 2026-09-04T13:54
---

- (constraint) Inherited initiative AD-1: staff operational writes remain in CouchDB SoR; public reads remain projected to MongoDB via worker.
- (constraint) Inherited initiative AD-2: frontend remains remote-first with no client-side offline write queue.
- (constraint) Inherited initiative AD-3: volunteer frontend preserves ui -> application -> data -> domain dependency direction.
- (constraint) Inherited initiative AD-4: shift assignments remain shelter-scoped in shelter_<code> databases.
- (constraint) Inherited initiative AD-7: persisted documents use typed ULID IDs, runtime validation, and monotonic schema_v.
- (constraint) CR-102 D-VOL-SHIFTLINK explicitly deferred linking shift_assignment to job.shifts[].id and identifies the required shift_assignment schema bump 3 -> 4.
- (direction) Fast-path deliverables are a feature architecture spine plus an implementation approval plan for coder agents and a Sol auditor; no job_shift_id implementation before human approval.
- (assumption) Use a single canonical child-to-parent reference shift_assignment.job_shift_id -> job.shifts[].id; do not store assignment ID arrays on embedded job shifts unless later evidence requires it.
- (assumption) Existing schema-v3 assignments need backward-compatible reads during migration; exact duty-window inference is acceptable only as a migration mechanism, not as the steady-state join key.
- (decision) Use shift_assignment.shift_id as the canonical child-to-parent reference to job.shifts[].id; preserve the embedded job field name id to avoid an unrelated job schema bump.
- (decision) Keep the relationship one-way. Do not store assignment IDs on job.shifts[]; reverse membership is queried by job_id plus shift_id so there is one source of truth.
- (decision) Bump shift_assignment schema_v 3 to 4. New writes require shift_id. Compatibility reads accept v3 only during migration and may use exact duty-window matching; ambiguous or unmatched rows are quarantined for manual resolution.
- (decision) Carry shift identity through public jobs, apply request, Mongo buffer, Couch job_application selected_shift, worker projections, ticket and schedule responses. Preserve date, time, and station as immutable display snapshots.
- (decision) Define the modal current-roster metric as distinct active seat holders for exactly one shift_id: assigned, standby, checked_in. Completed, no_show, and cancelled remain history and do not increment the current count.
- (constraint) Existing direct Couch staff path and BFF to FastAPI to Mongo buffer to worker to Couch public path must both converge on the same shift_id invariant; the legacy direct public BFF must be migrated or retired explicitly.
- (constraint) Implementation will use expand, backfill, and contract rollout. No code changes before owner approval of IMPLEMENTATION-PLAN.md.
- (decision) CR-104 reconciliation: canonical target removes dispatch/yellow state. Existing standby and dispatch_status remain compatibility-only during migration; new canonical writes follow the approved Job Board flow.
- (decision) CR-104 reconciliation: because runtime v3 does not match the approved documented v3 shape, use an amendment and monotonic v4 migrations rather than silently changing documents under the same schema_v.
- (constraint) CR-104 public apply sequence must honor the two-plane boundary: BFF to FastAPI to Mongo buffer to worker to CouchDB; direct public browser/BFF CouchDB writes are out of contract.
