# Env
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Deployment data (CouchDB volume)
/deployment

# Claude Code (keep shared settings.json, ignore local + ephemeral)
.claude/settings.local.json
.claude/worktrees/
