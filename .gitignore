# Dependencies
node_modules/

# Env
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Deployment data (CouchDB volume is ../deployment/couchdb — outside repo)
/deployment

# Claude Code (keep shared settings.json, ignore local + ephemeral)
.claude/settings.local.json
.claude/worktrees/
.claude/plans/

# Private
.private/

# Local agent scratch / temp publish bodies (not shared)
.scratch/
.tmp-*

# Local slides deck (not part of app repo)
/slides/
/frontend/slides/
/frontend/scripts/capture-all.ts

# Meeting notes (not shared with team)
docs/meetings/
couchdb-session.ini

.pnpm-store/

# Python (worker + packages)
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.antigravitycli/

node_modules/
backend/uv.lock
