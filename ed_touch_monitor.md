# 📱 คู่มือการตั้งค่าและใช้งาน Touch Screen: จออุตสาหกรรม ED-MONITOR-101C บน SmartShelter Kiosk

เอกสารคู่มือฉบับสมบูรณ์สำหรับการติดตั้ง ตั้งค่า และปรับเทียบพิกัดสัมผัสของจอสัมผัสอุตสาหกรรมขนาด 10.1 นิ้ว รุ่น **ED-MONITOR-101C (EDA Technology / EDATEC "Powered by Raspberry Pi")** สำหรับตู้ **SmartShelter Kiosk (Raspberry Pi 4 / 5 + Labwc/Wayland)**

---

## 📋 สารบัญ
1. [ข้อมูลจำเพาะฮาร์ดแวร์ ED-MONITOR-101C](#1-ข้อมูลจำเพาะฮาร์ดแวร์-ed-monitor-101c)
2. [การเชื่อมต่อสายและข้อควรระวังเรื่องระบบไฟ 12V](#2-การเชื่อมต่อสายและข้อควรระวังเรื่องระบบไฟ-12v)
3. [การตรวจจับของไดรเวอร์และ Kernel Linux](#3-การตรวจจับของไดรเวอร์และ-kernel-linux)
4. [สคริปต์ตั้งค่าอัตโนมัติ: `setup_monitor.sh`](#4-สคริปต์ตั้งค่าอัตโนมัติ-setup_monitorsh)
5. [ความละเอียด 1024x600 และแนวทางการปรับแต่ง UI](#5-ความละเอียด-1024x600-และแนวทางการปรับแต่ง-ui)
6. [การปรับแต่ง Chromium Kiosk สำหรับจอสัมผัส](#6-การปรับแต่ง-chromium-kiosk-สำหรับจอสัมผัส)
7. [บทวิเคราะห์เชิงวิศวกรรม (Engineering Analysis)](#7-บทวิเคราะห์เชิงวิศวกรรม)

---

## 1. ข้อมูลจำเพาะฮาร์ดแวร์ ED-MONITOR-101C

ข้อมูลทางเทคนิคของหน้าจอ **ED-MONITOR-101C (EDATEC)**:

| รายการ | รายละเอียดทางเทคนิค | ประโยชน์สำหรับ SmartShelter |
| :--- | :--- | :--- |
| **ขนาดหน้าจอ (Size)** | **10.1 นิ้ว** TFT LCD | ขนาดกะทัดรัด น้ำหนักเบา เหมาะกับเคาน์เตอร์จุดคัดกรองศูนย์พักพิง |
| **ความละเอียด (Resolution)** | **1024 x 600 พิกเซล** (Aspect Ratio 128:75) | สัดส่วนกว้าง ความคมชัดเพียงพอสำหรับการอ่านข้อมูลบัตร |
| **ความสว่าง (Luminance)** | **500 cd/m² (nits)** | **สว่างสูงมาก** สู้แสงแดดหรือไฟสปอร์ตไลต์ในเต็นท์ศูนย์พักพิงได้ชัดเจน |
| **มุมมองหน้าจอ (Viewing Angle)** | 85° รอบทิศทาง (ซ้าย/ขวา/บน/ล่าง) | ผู้ประสบภัยยืนมุมไหนก็มองเห็นคำแนะนำบนจอชัดเจน |
| **คอนทราสต์ (Contrast)** | 800:1, แสดงผล 16.7 ล้านสี (16.7M) | กราฟิกสีสดใส แยกแยะปุ่มกดและข้อความเตือนได้ง่าย |
| **ระบบสัมผัส (Touchscreen)** | **Capacitive Multi-touch (PCAP)** 5 จุดสัมผัส | ลื่นไหล แตะเบา ๆ ก็ติด ไม่ต้องออกแรงกด กระจกหน้าจอแข็งแกร่ง $\ge$ 6H |
| **พอร์ตเชื่อมต่อ Touch** | **USB Type-C (Driver-Free / Plug and Play)** | ใช้มาตรฐาน **USB HID-compliant** เสียบเข้า Raspberry Pi แล้วใช้งานได้ทันที |
| **พอร์ตสัญญาณภาพ** | 1 x Standard HDMI (Type-A) | ต่อผ่านสาย Micro-HDMI to Standard HDMI เข้าบอร์ด Pi |
| **พอร์ตสัญญาณเสียง** | 1 x 3.5mm Headphone Jack | รองรับการต่อลำโพงขยายเสียงสำหรับอ่านคำแนะนำขั้นตอน |
| **ระบบไฟเลี้ยง (Power)** | **DC Jack (5.5x2.5mm) 12V ~ 24V DC** | ⚠️ **ต้องต่อไฟ 12V แยก** กินไฟสูงสุด **10W** (Max) |
| **มาตรฐานความทนทาน** | ด้านหน้ากันน้ำและฝุ่นระดับ **IP65**, เฟรมอะลูมิเนียม | ป้องกันละอองน้ำ ฝุ่น และความชื้นในพื้นที่ภัยพิบัติ |
| **ขนาดและน้ำหนัก** | **257.7 x 159.7 x 29.2 mm**, หนักเพียง **940 กรัม** | เบามาก ติดตั้งง่าย รองรับขายึด VESA 75x75 mm และ Clamp หนีบขอบ |

---

## 2. การเชื่อมต่อสายและข้อควรระวังเรื่องระบบไฟ 12V

### 🔌 แผนผังการต่อสาย (Wiring Diagram)

```
[ ปลั๊กไฟ 220V ] ──> [ อะแดปเตอร์ 12V DC (1.5A - 2A ขึ้นไป) ] ──(DC Jack 5.5x2.5mm)──> [ ED-MONITOR-101C ]
                                                                                                │
[ Raspberry Pi 4/5 ] ───(สาย Micro-HDMI to Full HDMI)──────────────────────────────────────────┤ (สัญญาณภาพ)
                     └───(สาย USB-A to USB Type-C Data Cable)──────────────────────────────────┘ (สัญญาณทัชสกรีน)
```

### ⚠️ ข้อควรระวังระดับวิกฤต (Critical Cautions):
1. **ระบบไฟ 12V แยกต่างหาก:**
   - ตัวจอกินไฟ 10W ต้องการแรงดัน **12V ถึง 24V DC**
   - **ห้ามแปลงไฟ 5V จากพอร์ต USB ของบอร์ด Raspberry Pi มาจ่ายเข้าจอเด็ดขาด** เพราะแรงดันไม่พอและอาจทำให้ Raspberry Pi จ่ายไฟเกินจนระบบรีบูตวนซ้ำ
   - ควรใช้อะแดปเตอร์ไฟบ้าน 12V 2A หรือ 12V 3A (หัวแจ็คขนาด 5.5 x 2.5 mm) เสียบตรงเข้าช่องไฟของจอ
2. **สายส่งสัญญาณสัมผัส (Touch Cable):**
   - ใช้สาย **USB-A to USB Type-C ชนิดที่มีสายส่งข้อมูล (Data Cable)** เสียบจากพอร์ต USB 2.0 (พอร์ตสีดำ) หรือ 3.0 (พอร์ตสีน้ำเงิน) ของ Raspberry Pi เข้าที่ช่อง Type-C ของตัวจอ

---

## 3. การตรวจจับของไดรเวอร์และ Kernel Linux

เนื่องจากคอนโทรลเลอร์ทัชสกรีนของ ED-MONITOR-101C ส่งสัญญาณแบบ **USB HID Multi-Touch**:
- **Kernel Linux (รวมถึง Raspberry Pi OS Bookworm บน Labwc)** มีไดรเวอร์โมดูล `hid-multitouch` บิลต์อินมาให้อยู่แล้ว **ไม่ต้องดาวน์โหลดหรือติดตั้งไดรเวอร์ใด ๆ เพิ่มเติม (Driver-Free)**

### คำสั่งตรวจสอบฮาร์ดแวร์บน Raspberry Pi:
```bash
# 1. ตรวจสอบว่าพอร์ต USB เชื่อมต่อสำเร็จ
lsusb

# 2. ตรวจสอบ Kernel Log
dmesg | grep -i -E "touch|hid-multitouch"

# 3. ดูข้อมูลอุปกรณ์ผ่าน libinput (ระบบจัดการ Input บน Wayland)
sudo libinput list-devices
```

---

## 4. สคริปต์ตั้งค่าอัตโนมัติ: `setup_monitor.sh`

โปรเจกต์ได้เตรียมสคริปต์ [`setup_monitor.sh`](file:///home/jk/tent/scanner_client/setup_monitor.sh) ไว้ในโฟลเดอร์ `scanner_client` เพื่อช่วยจัดการเรื่องการหมุนจอ (Rotation), คาลิเบรตพิกัดสัมผัส (Calibration Matrix) และป้องกันจอดับแบบอัตโนมัติ

### วิธีใช้งาน:

1. **เปิดเมนูตั้งค่าแบบโต้ตอบ (Interactive Menu):**
   ```bash
   cd ~/tent/scanner_client
   ./setup_monitor.sh
   ```
   *จะมีเมนูให้เลือกตั้งค่า แนวนอน, แนวตั้ง 90°, แนวตั้ง 270°, ทดสอบทัช และป้องกันจอดับ*

2. **สั่งงานด่วนผ่านพารามิเตอร์ (CLI Shortcuts):**
   ```bash
   # ตั้งค่าเป็นแนวนอน 1024x600 (ค่าเริ่มต้น แนะนำสำหรับจอนี้)
   ./setup_monitor.sh --landscape

   # ตั้งค่าเป็นแนวตั้งหมุนขวา 90° (600x1024) พร้อมเซ็ต Touch Matrix อัตโนมัติ
   ./setup_monitor.sh --portrait

   # ตั้งค่าเป็นแนวตั้งหมุนซ้าย 270° (600x1024)
   ./setup_monitor.sh --portrait-left

   # ป้องกันหน้าจอดับ (Disable Screen Blanking / No Sleep)
   ./setup_monitor.sh --disable-blanking

   # ทดสอบการแตะสัมผัสหน้าจอแบบ Realtime
   ./setup_monitor.sh --test-touch

   # ตรวจสอบสถานะความละเอียดและ Touch Matrix ปัจจุบัน
   ./setup_monitor.sh --status
   ```


---

## 5. ความละเอียด 1024x600 และแนวทางการปรับแต่ง UI

จุดเด่นของจอรุ่น 10.1 นิ้วคือความกะทัดรัด แต่ความละเอียดหน้าจออยู่ที่ **1024 x 600 พิกเซล**:

### ประเด็นที่ต้องคำนึงถึง:
1. **ความสูงแนวตั้ง 600px:**
   - ในโหมดแนวนอน ความสูงของหน้าจอมีเพียง 600 พิกเซล หากองค์ประกอบในหน้าเว็บ (เช่น โลโก้, ขั้นตอน Wizard, ฟอร์ม) มี Margin/Padding ที่สูงเกินไป อาจทำให้ปุ่มกดกดยืนยันถูกดันตกขอบล่าง
2. **การตั้งค่าใน `scanner_client/.env`:**
   - ได้ปรับค่าเริ่มต้นสำหรับโหมด Windowed Debug ให้ตรงกับความละเอียดจริง:
     ```env
     WINDOW_WIDTH=1024
     WINDOW_HEIGHT=600
     ```
3. **การออกแบบ CSS Responsive ใน Svelte:**
   - ใช้ `h-screen overflow-hidden` บน Container หลักเพื่อป้องกันไม่ให้หน้าจอเลื่อนหลุดกรอบ
   - เพิ่ม Media Query สำหรับจอที่มีความสูง $\le$ 650px เพื่อลดระยะ Gap และขนาดย่อยของไอคอน

---

## 6. การปรับแต่ง Chromium Kiosk สำหรับจอสัมผัส

ใน [`scanner_client/app/manager.py`](file:///home/jk/tent/scanner_client/app/manager.py) ระบบได้กำหนด Flags สำหรับตู้ Kiosk จอสัมผัสไว้เรียบร้อยแล้ว:

```python
base_args = [
    "--disable-infobars",
    "--disable-session-crashed-bubble",
    "--disable-features=Translate,OverscrollHistoryNavigation",
    "--no-first-run",
    "--noerrdialogs",
    "--disable-pinch",                         # ป้องกันไม่ให้ผู้ใช้ถ่างนิ้วซูมหน้าจอจนเสียสัดส่วน
    "--overscroll-history-navigation=0",       # ป้องกันการปัดนิ้วซ้าย-ขวาเพื่อ Back/Forward
    "--check-for-update-interval=31536000",
    "--ozone-platform-hint=auto",              # รองรับ Wayland แท้บน Labwc
    "--disable-dev-shm-usage",                 # ป้องกัน shared memory เต็มบน RPi
    "--no-sandbox",                            # ป้องกัน renderer crash
    "--touch-events=enabled",                  # บังคับเปิดการรับส่ง Touch Event
]
```

### การซ่อนเคอร์เซอร์เมาส์ (Hide Mouse Cursor):
บนตู้ Kiosk จอสัมผัส ควรซ่อนลูกศรเมาส์เพื่อความสวยงาม โดยกำหนดใน CSS:
```css
body.kiosk-mode, body.kiosk-mode * {
    cursor: none !important;
    user-select: none !important; /* ป้องกันการแตะค้างแล้วขึ้นแถบไฮไลท์สีฟ้า */
    -webkit-user-select: none !important;
}
```

---

## 7. บทวิเคราะห์เชิงวิศวกรรม (Engineering Analysis)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    TECHNICAL ARCHITECTURAL ASSESSMENT                   │
├─────────────────────────────────────────────────────────────────────────┤
│ Monitor Model: ED-MONITOR-101C (EDA Technology)                         │
│ Screen Size  : 10.1-inch Industrial TFT LCD (1024 x 600)                │
│ Target Role  : SmartShelter Portable / Desktop Registration Kiosk       │
└─────────────────────────────────────────────────────────────────────────┘
```

### 🟢 จุดเด่นและข้อได้เปรียบ
1. **ความสว่างสูงมาก (500 nits):** สว่างคมชัดมาก ช่วยให้อ่านข้อความชัดเจนแม้อยู่ในเต็นท์กลางแจ้งที่มีแสงจ้า
2. **น้ำหนักเบาและคล่องตัวสูง (940 กรัม):** เหมาะอย่างยิ่งสำหรับการทำเป็นตู้ Kiosk แบบตั้งโต๊ะ หรือชุดลงทะเบียนเคลื่อนที่แบบพกพา (Mobile Deployment)
3. **ความทนทานระดับอุตสาหกรรม (IP65):** ทนต่อฝุ่น ละอองน้ำ และการกระแทกในพื้นที่ประสบภัย
4. **ความเข้ากันได้ 100% กับ Raspberry Pi:** ฮาร์ดแวร์และคอนโทรลเลอร์ทัชสกรีนถูกออกแบบและรับรองจาก Partner ของ Raspberry Pi โดยตรง

### 🔴 ข้อควรพึงระวังในการออกแบบระบบ
1. **จำกัดความสูงไว้ที่ 600 พิกเซล:** ต้องทดสอบหน้าจอ Kiosk ทุกหน้าเสมอว่าองค์ประกอบทั้งหมด (ปุ่ม, ข้อความ, ไอคอน) อยู่ครบในหน้าเดียวโดยไม่ต้องเลื่อนหน้าจอ
2. **การจัดเตรียมระบบไฟ 12V:** ต้องเผื่อพื้นที่สำหรับอะแดปเตอร์แปลงไฟ 12V ภายในโครงสร้างตู้ และแยกสายไฟให้เรียบร้อยเพื่อความปลอดภัย
